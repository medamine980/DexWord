import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
    },
    infoBtn: {
        width: 30,
        height: 30
    },
    infoImg: {
        height: "100%",
        aspectRatio: 1
    }
});

export default styles;